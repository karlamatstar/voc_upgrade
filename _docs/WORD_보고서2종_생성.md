# Word 보고서 2종 생성 기록

- 작업일: 2026-07-15
- 생성 위치: `D:\voc\quality_diagnosis\reports`
- 참조 자료: LLM Judge JSON·CSV·Markdown, pytest JSON·LOG, 테스트 케이스 JSON, 현재 코드 구조

## 생성 문서

1. `VOC_개선_QA_점검완료보고서.docx`
2. `VOC_분석_종합품질평가보고서.docx`

예시 문서는 포함할 내용의 범위만 참고했으며, 예시의 테스트 건수·PASS 수치·결함 내역은 사용하지 않았다. `_docs\example_Don't touch`의 파일은 수정하지 않았다.

## 반영한 실제 결과

- pytest: 76개 수집, 75 PASS, 0 FAIL, 0 ERROR, 1 SKIP
- pytest 수행시간: 581.866초
- LLM Judge: 20개 정의, 18개 처리, 16개 정식 채점
- 데이터 없음 PASS: TC-17, TC-18
- API 실패 N/A: 0건
- Judge 평균: 65.8점
- 즉시 보류: 2건
- 최종 품질 판정: 배포 보류

## 문서별 구성

### VOC 개선 QA 점검완료보고서

- 6페이지
- 표 12개
- 그래프 3개
- 프로젝트 목적, 6개 에이전트, pytest, Judge 현황, API 재시도·N/A, 결함·잔여 위험, 증적, 작성자 의견과 QA 결론 포함

### VOC 분석 종합품질평가보고서

- 6페이지
- 표 11개
- 그래프 4개
- 케이스별 총점, 판정 분포, 9개 평가 항목, 에이전트별 강점·약점, 상위·하위 사례, 즉시 보류, 응답시간, API 안정성, 개선 우선순위, 작성자 의견과 최종 결론 포함

## 데이터 부족 관련 의견

Judge 의견에서 8개 사례가 표본 수 또는 대표성 한계를 언급했다. 보고서에는 현재 `voc.csv`가 50건 규모이고 여러 질문에서 관련 사례가 1~2건에 그쳐 요약·비교·일반화 점수에 일정 부분 영향을 줬다고 작성했다.

다만 TC-13·14의 모호성 단정, TC-15의 검색 근거 이탈, TC-16의 복합 이슈 누락은 데이터 양만으로 설명하지 않고 에이전트 로직 개선이 필요한 문제로 분리했다.

## 검수 결과

- DOCX 파일 2개 정상 생성
- DOCX 내부 XML 읽기 성공
- 한글 제목·평균 65.8·데이터 한계·작성자 의견 포함 확인
- 차트를 외부 링크가 아닌 DOCX 내부 이미지로 포함
- 평가 항목 달성률 백분율 라벨 시각 검수 완료
- API 키·`.env` 값·민감정보 미포함

### 동적 표 빈칸 수정

초기 DOCX에서 동적으로 생성한 행 배열이 세로로 펼쳐지며 첫 번째 열에만 값이 들어가고 나머지 셀이 빈칸으로 남는 문제를 확인했다. 행 배열을 하나의 단위로 유지하도록 생성기를 수정한 뒤 두 문서를 다시 생성했다.

- QA 보고서: 데이터 행 54개, 빈 셀 0개
- 품질평가 보고서: 데이터 행 73개, 빈 셀 0개
- TC-17~20의 채점 제외 필드는 빈칸 대신 `평균 제외`, `해당 없음`으로 명시

### 테스트·Judge 설명 보강

- QA 보고서의 `독립 LLM Judge 수행 현황` 요약 카드를 4개에서 5개로 확장하고, 5번째에 `pytest 전용 TC-19·20` 2건을 표시
- TC-19는 Retriever 중단, TC-20은 CSV 누락 시 명확한 오류 안내를 검증하는 결정적 장애 테스트라 LLM Judge 대신 pytest로 분리했다는 이유를 본문에 추가
- `16개 채점 사례만 평균 반영`은 TC-01~16만 100점 채점, TC-17·18은 0건 PASS, TC-19·20은 pytest 전용이기 때문임을 표에 명시
- Anthropic이 3회 재시도 후에도 타임아웃·429·일시 장애로 실패하면 Judge 중단을 피하기 위해 OpenAI로 대체하고, 두 제공자 모두 실패 시 N/A 처리한다는 이유를 추가
- pytest SKIP 1건은 Summarizer(6003)가 가동 중이어서 서버 다운 시나리오를 재현할 수 없었던 것으로, 기능 미구현·테스트 실패가 아님을 추가
- 8개 pytest 파일을 파일명만 나열하지 않고 각 파일이 수행한 실제 검증 범위를 한글로 요약
- 품질평가 보고서의 품질 대시보드도 5개 카드로 확장하고 TC-19·20을 pytest 전용으로 분리한 이유를 직접 설명

## 재생성

동일한 로그를 기준으로 다시 생성할 때는 다음 스크립트를 사용한다.

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File D:\voc\quality_diagnosis\generate_word_reports.ps1
```
