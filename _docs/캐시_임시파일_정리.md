# 캐시·임시 파일 정리 기록

- 작업일: 2026-07-15
- 작업 범위: `D:\voc`
- 보호 대상: 소스, 가상환경 자체, 빌드 결과, 최종 DOCX, 실행 로그, `_docs\example_Don't touch`

## 삭제 결과

- 삭제한 캐시·임시 폴더: 460개
- 삭제한 캐시·임시 파일: 6,368개
- 삭제 항목:
  - 프로젝트와 `.venv` 내부의 `__pycache__`
  - `.pytest_cache`
  - `.pytest_temp*` 패턴의 접근 가능한 pytest 임시 폴더
  - `.pyc`, `.pyo`, `.tmp`, `.temp`, `.bak`
  - `dummy`, Word `~$` 임시 잠금 파일
  - DOCX에 이미 내장된 차트의 중간 생성물 `quality_diagnosis\reports\report_assets`

## 남은 항목

`D:\voc\.pytest_temp_retriever` 1개 폴더는 운영체제 ACL이 현재 계정의 읽기·소유권 변경·삭제를 모두 거부했다. 일반 삭제, 상승 실행, `takeown`, `icacls` 권한 복구를 시도했으나 모두 `Access is denied` 결과를 받았다.

관리자 권한 PowerShell에서 다음 순서로 소유권을 복구한 뒤 삭제해야 한다.

```powershell
takeown /F "D:\voc\.pytest_temp_retriever" /R /D Y
icacls "D:\voc\.pytest_temp_retriever" /grant "$env:USERNAME`:(OI)(CI)F" /T /C
Remove-Item -LiteralPath "D:\voc\.pytest_temp_retriever" -Recurse -Force
```

## 재검색 결과

- 접근 가능한 임시 파일: 0개
- 접근 가능한 캐시 폴더: `D:\voc\.pytest_temp_retriever` 1개만 남음
- 최종 보고서·로그·코드·보호 예시 폴더: 유지

## 추가 삭제 결과

후속 확인에서 `D:\voc\.pytest_temp_retriever`의 ACL이 `DWIT: FullControl`로 정상화된 것을 확인했다. 내부 pytest 임시 폴더 7개와 상위 폴더를 모두 삭제했으며, 최종 존재 여부는 `False`로 확인했다.
